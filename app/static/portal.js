const $=s=>document.querySelector(s),P=document.body.dataset.page,K='ssdBenchmarkDevice',Q='ssdBenchmarkQueue';const dev=()=>JSON.parse(localStorage.getItem(K)||'null'),queue=()=>JSON.parse(localStorage.getItem(Q)||'[]');const setQ=x=>localStorage.setItem(Q,JSON.stringify(x));const cap=x=>x?`${(x/1024**4).toFixed(2)} TiB`:'未知';const name=x=>({rand_read_4k:'4K 随机读取',seq_read_128k:'128K 顺序读取',rand_write_4k:'4K 随机写入',seq_write_128k:'128K 顺序写入',mixed_rw_4k:'4K 混合读写'})[x]||x;
function ensureNavigation(){const nav=document.querySelector('header nav');if(!nav)return;[['qd','/qd-scan','QD 扫描'],['advanced','/advanced','高级功能']].forEach(([key,href,label])=>{if(nav.querySelector(`[data-nav="${key}"]`))return;const link=document.createElement('a');link.dataset.nav=key;link.href=href;link.textContent=label;nav.append(link)})}
ensureNavigation();fetch('/health').then(r=>r.ok?$('#service-status').textContent='服务正常':Promise.reject()).catch(()=>$('#service-status').textContent='服务不可用');document.querySelector(`[data-nav="${P}"]`)?.classList.add('active');
async function scan(){let l=$('#device-list'),b=$('#scan-button');b.disabled=true;b.textContent='正在扫描…';try{let r=await fetch('/api/devices'),a=await r.json();if(!r.ok)throw Error(a.detail);$('#device-count').textContent=`发现 ${a.length} 块`;l.innerHTML='';a.forEach(d=>{let f=$('#device-template').content.cloneNode(true),c=f.querySelector('.device');f.querySelector('.device-name').textContent=d.name;f.querySelector('.device-model').textContent=d.model||'未知型号';f.querySelector('.device-size').textContent=cap(d.size_bytes);f.querySelector('.device-temp').textContent=d.temperature_c==null?'未知':`${d.temperature_c.toFixed(1)} °C`;f.querySelector('.device-serial').textContent=d.serial||'未知';f.querySelector('.device-state').textContent=d.safe_to_test?'可测试':'不可测试';f.querySelector('.device-reason').textContent=d.safe_to_test?'已通过安全准入。':d.safety_message||'未通过安全检查';if(d.safe_to_test){c.classList.add('safe');let choose=f.querySelector('.choose'),format=f.querySelector('.format');choose.onclick=()=>{localStorage.setItem(K,JSON.stringify(d));location='/tests'};format.onclick=async()=>{let confirmed=prompt(`危险操作：将执行 nvme format 并清空 /dev/${d.name}。\n请输入完整设备名 ${d.name} 确认：`);if(confirmed===null)return;if(confirmed!==d.name)return alert('设备名不匹配，已取消格式化。');format.disabled=true;format.textContent='正在格式化…';try{let r=await fetch(`/api/devices/${encodeURIComponent(d.name)}/format`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({confirm_device_name:confirmed})}),x=await r.json();if(!r.ok)throw Error(x.detail);alert(`格式化完成：${x.output}\n\n执行命令：${x.command.join(' ')}`);scan()}catch(e){alert(`格式化失败：${e.message}`);format.disabled=false;format.textContent='格式化 NVMe'}}}else f.querySelector('.format').remove();l.append(f)})}catch(e){l.innerHTML=`<div class="empty">扫描失败：${e.message}</div>`}finally{b.disabled=false;b.textContent='↻ 重新扫描'}}
function fio(){let o={runtime_seconds:+$('#runtime').value,ramp_time_seconds:+$('#ramp').value,iodepth:+$('#iodepth').value,numjobs:+$('#numjobs').value,ioengine:$('#ioengine').value,direct:$('#direct').checked},bs=$('#block-size').value.trim();if(bs)o.bs=bs;$('#extra-options').value.split('\n').map(x=>x.trim()).filter(Boolean).forEach(x=>{let i=x.indexOf('=');if(i<1)throw Error(`参数格式错误：${x}`);let k=x.slice(0,i),v=x.slice(i+1);o[k]=v==='true'?true:v==='false'?false:isNaN(+v)||v===''?v:+v});return o}
function tests(){let d=dev(),selected=()=>document.querySelector('input[name=test]:checked').value,mixed=()=>selected()==='mixed_rw_4k',write=()=>mixed()||selected().includes('write'),slider=$('#mixed-read-ratio'),number=$('#mixed-read-number'),syncMixed=value=>{let ratio=Math.max(0,Math.min(100,Math.round(Number(value)||0)));slider.value=ratio;number.value=ratio;$('#mixed-read-display').textContent=`${ratio}%`;$('#mixed-ratio-summary').textContent=`${ratio}% 读取 / ${100-ratio}% 写入`};$('#selected-device').textContent=d?`${d.name} · ${d.model||'NVMe SSD'}`:'尚未选择';slider.oninput=()=>syncMixed(slider.value);number.oninput=()=>{if(number.value!=='')syncMixed(number.value)};number.onchange=()=>syncMixed(number.value);$('#test-grid').onchange=()=>{$('#write-confirm').classList.toggle('hidden',!write());$('#mixed-config').classList.toggle('hidden',!mixed());document.querySelectorAll('.test').forEach(x=>x.classList.toggle('selected',x.querySelector('input').checked));syncMixed(slider.value)};let payload=()=>{if(!d)throw Error('请先选择测试设备。');if(write()&&!$('#confirm-destructive').checked)throw Error('请确认写入风险。');let o=fio();if(mixed())o.rwmixread=+slider.value;if(o.ramp_time_seconds>o.runtime_seconds)throw Error('预热时长不能大于测试时长。');return{device_name:d.name,test_name:selected(),confirm_destructive:write(),fio_options:o}};$('#confirm-device').textContent=d?.name||'目标盘';$('#start-button').onclick=async()=>{try{let r=await fetch('/api/tests',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload())}),x=await r.json();if(!r.ok)throw Error(x.detail);localStorage.setItem('ssdBenchmarkTask',x.id);location=`/results?task_id=${x.id}`}catch(e){$('#form-message').textContent=e.message}};$('#queue-button').onclick=()=>{try{let q=queue();q.push(payload());setQ(q);location='/queue'}catch(e){$('#form-message').textContent=e.message}}}
function batch(){let d=dev();$('#selected-device').textContent=d?`${d.name} · ${d.model||'NVMe SSD'}`:'尚未选择';let render=()=>{let q=queue(),l=$('#batch-list');$('#queue-count').textContent=`${q.length} 项`;$('#start-batch-button').disabled=!q.length;if(!q.length){l.innerHTML='<li class="empty">队列为空。请前往“单项测试”添加测试项。</li>';return}l.innerHTML='';q.forEach((x,i)=>{let z=document.createElement('li');z.innerHTML=`<span><b>${i+1}. ${name(x.test_name)}</b><small>${x.fio_options.runtime_seconds} 秒 · QD ${x.fio_options.iodepth}</small></span><button data-i="${i}">移除</button>`;l.append(z)});l.querySelectorAll('button').forEach(b=>b.onclick=()=>{let q=queue();q.splice(+b.dataset.i,1);setQ(q);render()})};$('#start-batch-button').onclick=async()=>{try{if(!d)throw Error('请先选择设备');let r=await fetch('/api/batches',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({device_name:d.name,tests:queue().map(x=>({test_name:x.test_name,confirm_destructive:x.confirm_destructive,fio_options:x.fio_options}))})}),x=await r.json();if(!r.ok)throw Error(x.detail);localStorage.setItem('ssdBenchmarkTask',x.task_ids[0]);location=`/results?task_id=${x.task_ids[0]}`}catch(e){$('#batch-message').textContent=e.message}};render()}
function result(){let t=$('#task-input'),id=new URLSearchParams(location.search).get('task_id')||'';t.value=id;let run=async()=>{let n=t.value;if(!n)return $('#query-message').textContent='请输入任务编号';try{let r=await fetch(`/api/results/${n}`),x=await r.json();if(!r.ok)throw Error(x.detail);localStorage.setItem('ssdBenchmarkTask',n);$('#result-panel').classList.remove('hidden');$('#task-id').textContent=`任务 #${n}`;$('#task-state').textContent=x.status==='completed'?'测试完成':x.status==='failed'?'测试失败':`正在执行：${x.progress_phase}`;let qdNote=$('#qd-scan-note');if(x.batch_type==='qd_scan'){qdNote.classList.remove('hidden');$('#qd-scan-detail').textContent=`批次 #${x.batch_id} · 当前队列深度 QD ${x.qd_value}`;$('#task-state').textContent+=` · QD 扫描（QD ${x.qd_value}）`}else qdNote.classList.add('hidden');$('#progress-wrap').classList.remove('hidden');$('#progress-label').textContent=x.progress_phase;$('#progress-percent').textContent=`${x.progress_percent}%`;$('#progress-detail').textContent=`已运行 ${x.elapsed_seconds} / ${x.total_seconds} 秒`;$('#progress-fill').style.width=`${x.progress_percent}%`;let o=x.fio_options||{},g=$('#parameter-grid');$('#parameter-panel').classList.remove('hidden');$('#parameter-test-name').textContent=name(x.test_name);g.innerHTML='';[['测试设备',`${x.device_name} · ${x.device_path}`],['测试类型',name(x.test_name)],['测试时长',`${o.runtime_seconds} 秒`],['预热时长',`${o.ramp_time_seconds} 秒`],['队列深度',o.iodepth],['并发任务',o.numjobs],['I/O 引擎',o.ioengine],['绕过缓存',o.direct?'direct=1':'direct=0']].concat(Object.entries(o).filter(([k])=>!['runtime_seconds','ramp_time_seconds','iodepth','numjobs','ioengine','direct'].includes(k)).map(([k,v])=>[`自定义 · ${k}`,String(v)])).forEach(([k,v])=>{let a=document.createElement('article'),b=document.createElement('span'),c=document.createElement('b');b.textContent=k;c.textContent=v;a.append(b,c);g.append(a)});$('#fio-command').textContent=(x.fio_command||[]).join(' ');document.querySelectorAll('#report-downloads a').forEach(link=>{let ready=x.status==='completed'&&x.result;link.classList.toggle('disabled',!ready);link.href=ready?`/api/reports/${n}?report_format=${link.dataset.format}`:'#';link.onclick=ready?null:e=>{e.preventDefault();$('#query-message').textContent='测试完成后才可以下载报告。'}});if(x.status==='completed'&&x.result){['iops','bw_mib_s','latency_avg_us','latency_p99_us','cpu_user_pct','cpu_system_pct'].forEach(k=>{$('#metric-'+({bw_mib_s:'bw',latency_avg_us:'latency',latency_p99_us:'p99',cpu_user_pct:'user-cpu',cpu_system_pct:'system-cpu'}[k]||k)).textContent=(x.result[k]??'—')+(k==='bw_mib_s'?' MiB/s':k.includes('latency')?' μs':k.includes('cpu')?' %':'')});$('#metric-grid').classList.remove('hidden');return}if(x.status==='failed'){let e=$('#error-box');e.textContent=x.error_message||'任务失败';e.classList.remove('hidden');return}setTimeout(run,2500)}catch(e){$('#query-message').textContent=e.message}};$('#query-button').onclick=run;if(id)run()}
function taskList(){let l=$('#task-list'),chosen=new Set(),all=$('#select-all-tasks'),bulk=$('#delete-selected-tasks'),count=$('#selected-task-count'),status=x=>({queued:'排队中',running:'运行中',completed:'已完成',failed:'执行失败'})[x]||x,terminal=x=>['completed','failed'].includes(x),update=items=>{let eligible=items.filter(x=>terminal(x.status)).map(x=>x.id);chosen=new Set([...chosen].filter(id=>eligible.includes(id)));count.textContent=chosen.size;bulk.disabled=!chosen.size;all.checked=eligible.length>0&&eligible.every(id=>chosen.has(id));all.indeterminate=chosen.size>0&&!all.checked},render=async()=>{try{let r=await fetch('/api/tasks'),a=await r.json();if(!r.ok)throw Error(a.detail);$('#task-total').textContent=`共 ${a.length} 项`;l.innerHTML='';if(!a.length){l.innerHTML='<div class="empty">暂未创建测试任务。</div>';update([]);return}a.forEach(x=>{let row=document.createElement('article');row.className='task-row';row.innerHTML='<span class="task-select-cell"></span><span class="task-row-id"></span><span class="task-row-main"><b></b><small></small></span><span class="task-row-progress"></span><span class="task-row-status"></span><span class="task-row-actions"><button type="button" class="task-view">查看 →</button></span>';row.querySelector('.task-row-id').textContent=`#${x.id}`;row.querySelector('.task-row-main b').textContent=name(x.test_name);row.querySelector('.task-row-main small').textContent=x.batch_type==='qd_scan'?`${x.device_name} · QD 扫描 #${x.batch_id} · QD ${x.qd_value}`:x.device_name;row.classList.toggle('qd-task-row',x.batch_type==='qd_scan');row.querySelector('.task-row-progress').textContent=`${x.progress_percent}% · ${x.progress_phase}`;row.querySelector('.task-row-status').textContent=status(x.status);row.querySelector('.task-view').onclick=()=>{localStorage.setItem('ssdBenchmarkTask',x.id);location=`/results?task_id=${x.id}`};if(terminal(x.status)){let box=document.createElement('input');box.type='checkbox';box.className='task-select';box.checked=chosen.has(x.id);box.setAttribute('aria-label',`选择任务 #${x.id}`);box.dataset.taskId=x.id;box.onchange=()=>{box.checked?chosen.add(x.id):chosen.delete(x.id);update(a)};row.querySelector('.task-select-cell').append(box);let del=document.createElement('button');del.type='button';del.className='task-delete';del.textContent='删除';del.onclick=async()=>{if(!confirm(`确定删除任务 #${x.id} 吗？
该任务的测试结果将一并删除，无法恢复。`))return;del.disabled=true;try{let r=await fetch(`/api/tasks/${x.id}`,{method:'DELETE'});if(!r.ok){let e=await r.json();throw Error(e.detail)}chosen.delete(x.id);render()}catch(e){alert(`删除失败：${e.message}`);del.disabled=false}};row.querySelector('.task-row-actions').append(del)}else{let note=document.createElement('small');note.className='task-delete-disabled';note.textContent='不可删除';row.querySelector('.task-row-actions').append(note)}l.append(row)});update(a)}catch(e){l.innerHTML=`<div class="empty">任务列表加载失败：${e.message}</div>`}};all.onchange=()=>{let selected=all.checked;document.querySelectorAll('.task-select').forEach(box=>selected?chosen.add(+box.dataset.taskId):chosen.delete(+box.dataset.taskId));render()};bulk.onclick=async()=>{let ids=[...chosen];if(!ids.length)return;if(!confirm(`确定删除选中的 ${ids.length} 条任务吗？
这些任务的测试结果将一并删除，无法恢复。`))return;bulk.disabled=true;try{let r=await fetch('/api/tasks',{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({task_ids:ids})});if(!r.ok){let e=await r.json();throw Error(e.detail)}chosen.clear();render()}catch(e){alert(`批量删除失败：${e.message}`);bulk.disabled=false}};render();setInterval(render,5000)}
if(P==='devices'){$('#scan-button').onclick=scan;scan()}if(P==='tests')tests();if(P==='queue')batch();if(P==='task-list')taskList();if(P==='results')result();
function qd(){let d=dev(),test=$('#qd-test'),confirm=$('#qd-confirm');$('#qd-device').textContent=d?`${d.name} · ${d.model||'NVMe SSD'}`:'尚未选择';let risky=()=>test.value.includes('write');test.onchange=()=>confirm.classList.toggle('hidden',!risky());$('#qd-start').onclick=async()=>{try{if(!d)throw Error('请先选择测试设备。');if(risky()&&!$('#qd-destructive').checked)throw Error('写入型 QD 扫描必须确认清除数据风险。');let qds=$('#qd-values').value.split(',').map(x=>+x.trim()).filter(x=>Number.isInteger(x)&&x>0);if(!qds.length)throw Error('请填写有效 QD。');let r=await fetch('/api/qd-scans',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({device_name:d.name,test_name:test.value,qd_values:qds,runtime_seconds:+$('#qd-runtime').value,ramp_time_seconds:+$('#qd-ramp').value,numjobs:+$('#qd-numjobs').value,ioengine:$('#qd-ioengine').value,confirm_destructive:risky()})}),x=await r.json();if(!r.ok)throw Error(x.detail);$('#qd-message').textContent=`已创建批次 #${x.batch_id}`;localStorage.setItem('ssdBenchmarkTask',x.task_ids[0]);location=`/results?task_id=${x.task_ids[0]}`}catch(e){$('#qd-message').textContent=e.message}}}
if(P==='qd')qd();

async function api(path,options){let r=await fetch(path,options),x=await r.json().catch(()=>({}));if(!r.ok)throw Error(x.detail||'请求失败');return x}
function esc(value){return String(value??'—').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
async function deviceOptions(selector){let list=await api('/api/devices'),select=$(selector);select.innerHTML=list.map(d=>`<option value="${esc(d.name)}">${esc(d.name)} · ${esc(d.model||'NVMe SSD')}</option>`).join('');let current=dev();if(current&&list.some(d=>d.name===current.name))select.value=current.name;return list}
function healthLogs(){let select=$('#health-device'),message=$('#health-message');let render=async()=>{let n=select.value;if(!n)return;let [history,logs,findings]=await Promise.all([api(`/api/devices/${encodeURIComponent(n)}/health-history`),api(`/api/devices/${encodeURIComponent(n)}/logs`),api(`/api/diagnostics?device_name=${encodeURIComponent(n)}`)]);let newest=history.items.at(-1),summary=$('#health-summary');summary.innerHTML=newest?[['健康评分',newest.health_score],['等级',newest.grade],['温度',newest.temperature_c==null?'—':`${newest.temperature_c} °C`],['已用寿命',newest.percentage_used==null?'—':`${newest.percentage_used}%`],['介质错误',newest.media_errors??'—'],['通电时长',newest.power_on_hours==null?'—':`${newest.power_on_hours} h`]].map(([k,v])=>`<article><span>${k}</span><b>${esc(v)}</b></article>`).join(''):'<div class="empty">暂无快照，请点击“采集 SMART 健康快照”。</div>';let warnings=[...(newest?.warnings||[]),...findings.filter(x=>x.status!=='resolved').map(x=>x.message||x.title)].filter(Boolean),box=$('#health-warnings');box.classList.toggle('hidden',!warnings.length);box.innerHTML=warnings.map(x=>`<p>${esc(x)}</p>`).join('');$('#health-history').innerHTML=history.items.length?`采集 ${history.items.length} 次 · 温度 ${history.thermal_trend.minimum_c??'—'}–${history.thermal_trend.maximum_c??'—'} °C · ${esc(history.thermal_trend.advice)}`:'暂无历史健康快照。';$('#health-logs').innerHTML=logs.length?logs.map(x=>`<div><b>${esc(x.log_type)}</b><small>${esc(x.status)} · ${esc(x.created_at)} · ${x.byte_size??0} bytes</small></div>`).join(''):'暂无日志归档。'};deviceOptions('#health-device').then(render).catch(e=>message.textContent=e.message);select.onchange=()=>render().catch(e=>message.textContent=e.message);$('#health-capture').onclick=async()=>{message.textContent='正在采集 SMART…';try{await api(`/api/devices/${encodeURIComponent(select.value)}/health`,{method:'POST'});message.textContent='SMART 健康快照已保存。';await render()}catch(e){message.textContent=e.message}};$('#health-diagnose').onclick=async()=>{message.textContent='正在运行诊断…';try{let x=await api(`/api/devices/${encodeURIComponent(select.value)}/diagnostics`,{method:'POST'});message.textContent=`诊断完成，新增 ${x.finding_count} 条结论。`;await render()}catch(e){message.textContent=e.message}};document.querySelectorAll('[data-log]').forEach(button=>button.onclick=async()=>{message.textContent=`正在采集 ${button.textContent}…`;button.disabled=true;try{let x=await api(`/api/devices/${encodeURIComponent(select.value)}/logs/${button.dataset.log}`,{method:'POST'});message.textContent=x.status==='completed'?'日志已归档。':`日志采集失败：${x.error_message||'未知错误'}`;await render()}catch(e){message.textContent=e.message}finally{button.disabled=false}})}
function comparison(){let chosen=new Set(),list=$('#comparison-candidates'),run=$('#comparison-run'),message=$('#comparison-message');let load=async()=>{try{let tasks=await api('/api/tasks'),items=tasks.filter(x=>x.status==='completed');list.innerHTML=items.length?items.map(x=>`<label class="compare-choice"><input type="checkbox" value="${x.id}" ${chosen.has(x.id)?'checked':''}><span><b>#${x.id} · ${esc(name(x.test_name))}</b><small>${esc(x.device_name)} · ${x.progress_percent}%</small></span></label>`).join(''):'暂无可用于对标的已完成任务。';list.querySelectorAll('input').forEach(i=>i.onchange=()=>{i.checked?chosen.add(+i.value):chosen.delete(+i.value);run.disabled=chosen.size<2});run.disabled=chosen.size<2}catch(e){list.textContent=e.message}};$('#comparison-refresh').onclick=load;run.onclick=async()=>{try{let x=await api(`/api/comparisons/tasks?task_ids=${[...chosen].join(',')}`),rows=x.items||[];$('#comparison-result').classList.remove('hidden');$('#comparison-summary').textContent=`共比较 ${x.summary?.count||0} 项，第一名：${x.summary?.winner?`#${x.summary.winner.task_id}`:'—'}`;$('#comparison-table').innerHTML=rows.map(r=>`<tr><td>${r.rank}</td><td>#${r.task_id} · ${esc(name(r.test_name))}</td><td>${esc(r.device_name)}</td><td>${r.iops??'—'}</td><td>${r.bandwidth??'—'}</td><td>${r.latency??'—'}</td><td>${r.latency??'—'}</td></tr>`).join('')}catch(e){message.textContent=e.message}};load()}
function operations(){let msg=$('#plan-message'),plans=$('#plan-list');let renderPlans=async()=>{let rows=await api('/api/scheduled-plans');plans.innerHTML=rows.length?rows.map(x=>`<div class="plan-row"><span><b>${esc(x.name)}</b><small>${esc(x.device_name)} · 每 ${x.interval_minutes} 分钟 · ${x.enabled?'已启用':'已暂停'} · 下次 ${esc(x.next_run_at||'—')}</small></span><button data-toggle="${x.id}" data-enabled="${!x.enabled}">${x.enabled?'暂停':'启用'}</button><button class="danger" data-delete="${x.id}">删除</button></div>`).join(''):'暂无定时测试计划。';plans.querySelectorAll('[data-toggle]').forEach(b=>b.onclick=async()=>{await api(`/api/scheduled-plans/${b.dataset.toggle}/enabled`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({enabled:b.dataset.enabled==='true'})});renderPlans()});plans.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(confirm('确定删除此计划吗？')){let r=await fetch(`/api/scheduled-plans/${b.dataset.delete}`,{method:'DELETE'});if(!r.ok)throw Error('删除失败');renderPlans()}})};Promise.all([deviceOptions('#plan-device'),api('/api/platform/compatibility')]).then(([,compat])=>{$('#compatibility-list').innerHTML=Object.entries(compat).map(([k,v])=>`<div><b>${esc(k)}</b><small>${esc(typeof v==='object'?JSON.stringify(v):v)}</small></div>`).join('')||'未返回平台信息。'}).catch(e=>msg.textContent=e.message);$('#plan-create').onclick=async()=>{try{let payload={name:$('#plan-name').value.trim(),device_name:$('#plan-device').value,interval_minutes:+$('#plan-interval').value,tests:[{test_name:$('#plan-test').value,confirm_destructive:false,fio_options:{runtime_seconds:60,ramp_time_seconds:10,iodepth:32,numjobs:1,ioengine:'io_uring',direct:true}}]};let x=await api('/api/scheduled-plans',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});msg.textContent=`已创建计划 #${x.id}。`;$('#plan-name').value='';renderPlans()}catch(e){msg.textContent=e.message}};renderPlans().catch(e=>msg.textContent=e.message)}
if(location.pathname==='/health-logs')healthLogs();if(location.pathname==='/comparison')comparison();if(location.pathname==='/operations')operations();
