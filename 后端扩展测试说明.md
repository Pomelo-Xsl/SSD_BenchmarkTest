# SSD BenchmarkTest 后端扩展测试说明

## 覆盖模块

- fio 单项、批量与混合读写负载参数验证
- 结果快照、基线、评分、趋势与告警
- NVMe SMART 健康、日志归档与诊断规则
- 定时计划、保留策略、跨系统依赖探测
- 实时曲线聚合与多任务对标排名

## 执行

```bash
.venv/bin/python -m pytest -q
python3 scripts/code_statistics.py
```

## 行数口径

统计 `app/` 下全部 Python 文件；不含虚拟环境、生成文件和测试文件。
